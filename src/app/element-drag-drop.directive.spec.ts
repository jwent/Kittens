import { ElementDragDropDirective } from './element-drag-drop.directive';

describe('ElementDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
